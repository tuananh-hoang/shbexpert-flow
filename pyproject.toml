[project]
name = "dashmint-synthetic-data"
version = "0.1.0"
description = "Dataset-first synthetic banking data pipeline"
requires-python = ">=3.11"
dependencies = ["openpyxl>=3.1"]

[project.scripts]
mockdata = "synthetic_data_pipeline.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
